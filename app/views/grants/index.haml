%h1 Listing grants
%table
  %thead
    %tr
      %th Program title
      %th Institution
      %th Department
      %th Program description
      %th Subdomain
      %th{colspan: "3"}
  %tbody
    - @grants.each do |grant|
      %tr
        %td= grant.program_title
        %td= grant.institution
        %td= grant.subdomain
        %td= link_to 'Show', grant_path(grant)
        %td= link_to 'Edit', edit_grant_path(grant)
        %td= link_to 'Destroy', grant, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Grant', new_grant_path