= render "layouts/navbar"

- if settings_filled_in?
  %header.container
    .header-inner.main-header
      %h1{ :id => "header_link_1" }
        #{link_to(Setting[:university], Setting[:university_url])}, #{link_to(Setting[:department], Setting[:department_url])}
      %h2{ :id => "header_link_2" }
        = link_to("#{Setting[:app_title]} (#{Setting[:program_start_date]} - #{Setting[:program_end_date]})", Setting[:program_url])
