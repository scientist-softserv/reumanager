.row
  .col-2
  .col-8
    %h2.float-left Applicants
    %button.float-right= link_to 'Export All as CSV', reu_program_applicants_path(format: 'csv')
  .col-2
.row
  .col-2
  .col-8
    %table.table
      %thead
        %tr
          %th Name
          %th State
          %th Actions
      %tbody
        - @applicants.each do |applicant|
          %tr
            %td= link_to applicant.applicant_datum.data["profile"]["first_name"] + " " + applicant.applicant_datum.data["profile"]["last_name"], reu_program_applicant_path(applicant)
            %td= applicant.state
            %td.actions
              = link_to 'PDF', edit_reu_program_applicant_path(applicant) |
              %button= link_to 'Export CSV', reu_program_applicant_path(applicant, format: 'csv')
    .col-2